{
  "name": "northwindbrowser",
  "version": "1.0.0",
  "description": "Northwind database browser app",
  "main": "index.js",
  "scripts": {
    "start": "fiori run --config ui5.yaml --open index.html",
    "start-mock": "fiori run --config ui5-mock.yaml --open test/mockServer.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "bingyusandor"
  },
  "keywords": [
    "northwind"
  ],
  "author": "Sani",
  "license": "ISC",
  "devDependencies": {
    "@sap/ux-ui5-tooling": "^1.12.2"
  },
  "ui5": {
    "dependencies": [
      "@sap/ux-ui5-tooling"
    ]
  },
  "dependencies": {
    "@ui5/cli": "^3.9.1"
  }
}
